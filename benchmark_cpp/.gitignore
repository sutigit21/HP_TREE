# Build artifacts
build/

# Generated binary datasets (regenerable via python/generate_datasets.py)
datasets/*.bin

# Benchmark result JSON / logs — now tracked for reproducibility
# results/*.json
# results/*.log
